// AUTO-GENERATED by cargo xtask codegen -- DO NOT EDIT
// WASI C-ABI exports for non-JS WASM runtimes (wasmtime, wasmer)

#include "occt_kernel.h"

#include <cstdlib>
#include <cstring>
#include <limits>
#include <new>
#include <string>
#include <vector>

// === Global state ===

static OcctKernel* g_kernel = nullptr;

// Error state
static bool g_has_error = false;
static std::string g_error_msg;

#define CLEAR_ERROR() g_has_error = false
#define SET_ERROR(msg) do { g_has_error = true; g_error_msg = (msg); } while(0)

// Result buffers for complex return types
static std::string g_string_buf;
static std::vector<uint32_t> g_vec_u32_buf;
static std::vector<double> g_vec_f64_buf;
static std::vector<int32_t> g_vec_i32_buf;
static MeshData g_mesh_buf;
static MeshBatchData g_mesh_batch_buf;
static EdgeData g_edge_buf;
static BBoxData g_bbox_buf;
static NurbsCurveData g_nurbs_buf;
static EvolutionData g_evo_buf;
static ProjectionData g_proj_buf;
static XCAFLabelInfo g_label_info_buf;

extern "C" {

// === Lifecycle ===

int32_t occt_init() {
    if (g_kernel) return -1;
    try {
        g_kernel = new OcctKernel();
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

void occt_destroy() {
    delete g_kernel;
    g_kernel = nullptr;
}

// Extension point for out-of-tree facade files (C linkage: inside extern "C").
OcctKernel* occt_wasi_kernel_for_extensions() {
    return g_kernel;
}

// === Error handling ===

int32_t occt_has_error() { return g_has_error ? 1 : 0; }

const char* occt_get_error() { return g_error_msg.c_str(); }

uint32_t occt_get_error_len() { return static_cast<uint32_t>(g_error_msg.size()); }

void occt_clear_error() { g_has_error = false; g_error_msg.clear(); }

// === Memory management ===

void* occt_alloc(uint32_t size) { return malloc(size); }

void occt_free(void* ptr) { free(ptr); }

// === Result buffer accessors ===

const char* occt_get_string_result() { return g_string_buf.data(); }
uint32_t occt_get_string_result_len() { return static_cast<uint32_t>(g_string_buf.size()); }

const uint32_t* occt_get_vec_u32_result() { return g_vec_u32_buf.data(); }
uint32_t occt_get_vec_u32_result_len() { return static_cast<uint32_t>(g_vec_u32_buf.size()); }

const double* occt_get_vec_f64_result() { return g_vec_f64_buf.data(); }
uint32_t occt_get_vec_f64_result_len() { return static_cast<uint32_t>(g_vec_f64_buf.size()); }

const int32_t* occt_get_vec_i32_result() { return g_vec_i32_buf.data(); }
uint32_t occt_get_vec_i32_result_len() { return static_cast<uint32_t>(g_vec_i32_buf.size()); }

// BBox result
double occt_get_bbox_xmin() { return g_bbox_buf.xmin; }
double occt_get_bbox_ymin() { return g_bbox_buf.ymin; }
double occt_get_bbox_zmin() { return g_bbox_buf.zmin; }
double occt_get_bbox_xmax() { return g_bbox_buf.xmax; }
double occt_get_bbox_ymax() { return g_bbox_buf.ymax; }
double occt_get_bbox_zmax() { return g_bbox_buf.zmax; }

// Mesh result
const float* occt_get_mesh_positions() { return g_mesh_buf.positions; }
int32_t occt_get_mesh_positions_len() { return g_mesh_buf.positionCount; }
const float* occt_get_mesh_normals() { return g_mesh_buf.normals; }
int32_t occt_get_mesh_normals_len() { return g_mesh_buf.normalCount; }
const uint32_t* occt_get_mesh_indices() { return g_mesh_buf.indices; }
int32_t occt_get_mesh_indices_len() { return g_mesh_buf.indexCount; }
const int32_t* occt_get_mesh_face_groups() { return g_mesh_buf.faceGroups; }
int32_t occt_get_mesh_face_groups_len() { return g_mesh_buf.faceGroupCount; }

// MeshBatch result
const float* occt_get_mesh_batch_positions() { return g_mesh_batch_buf.positions; }
int32_t occt_get_mesh_batch_positions_len() { return g_mesh_batch_buf.positionCount; }
const float* occt_get_mesh_batch_normals() { return g_mesh_batch_buf.normals; }
int32_t occt_get_mesh_batch_normals_len() { return g_mesh_batch_buf.normalCount; }
const uint32_t* occt_get_mesh_batch_indices() { return g_mesh_batch_buf.indices; }
int32_t occt_get_mesh_batch_indices_len() { return g_mesh_batch_buf.indexCount; }
const int32_t* occt_get_mesh_batch_shape_offsets() { return g_mesh_batch_buf.shapeOffsets; }
int32_t occt_get_mesh_batch_shape_count() { return g_mesh_batch_buf.shapeCount; }

// Edge result
const float* occt_get_edge_points() { return g_edge_buf.points; }
int32_t occt_get_edge_points_len() { return g_edge_buf.pointCount; }
const int32_t* occt_get_edge_groups() { return g_edge_buf.edgeGroups; }
int32_t occt_get_edge_groups_len() { return g_edge_buf.edgeGroupCount; }

// NURBS result
int32_t occt_get_nurbs_degree() { return g_nurbs_buf.degree; }
int32_t occt_get_nurbs_rational() { return g_nurbs_buf.rational ? 1 : 0; }
int32_t occt_get_nurbs_periodic() { return g_nurbs_buf.periodic ? 1 : 0; }
const double* occt_get_nurbs_knots() { return g_nurbs_buf.knots.data(); }
uint32_t occt_get_nurbs_knots_len() { return static_cast<uint32_t>(g_nurbs_buf.knots.size()); }
const int32_t* occt_get_nurbs_multiplicities() { return reinterpret_cast<const int32_t*>(g_nurbs_buf.multiplicities.data()); }
uint32_t occt_get_nurbs_multiplicities_len() { return static_cast<uint32_t>(g_nurbs_buf.multiplicities.size()); }
const double* occt_get_nurbs_poles() { return g_nurbs_buf.poles.data(); }
uint32_t occt_get_nurbs_poles_len() { return static_cast<uint32_t>(g_nurbs_buf.poles.size()); }
const double* occt_get_nurbs_weights() { return g_nurbs_buf.weights.data(); }
uint32_t occt_get_nurbs_weights_len() { return static_cast<uint32_t>(g_nurbs_buf.weights.size()); }

// Evolution result
uint32_t occt_get_evo_result_id() { return g_evo_buf.resultId; }
const int32_t* occt_get_evo_modified() { return reinterpret_cast<const int32_t*>(g_evo_buf.modified.data()); }
uint32_t occt_get_evo_modified_len() { return static_cast<uint32_t>(g_evo_buf.modified.size()); }
const int32_t* occt_get_evo_generated() { return reinterpret_cast<const int32_t*>(g_evo_buf.generated.data()); }
uint32_t occt_get_evo_generated_len() { return static_cast<uint32_t>(g_evo_buf.generated.size()); }
const int32_t* occt_get_evo_deleted() { return reinterpret_cast<const int32_t*>(g_evo_buf.deleted.data()); }
uint32_t occt_get_evo_deleted_len() { return static_cast<uint32_t>(g_evo_buf.deleted.size()); }

// Projection result
uint32_t occt_get_proj_visible_outline() { return g_proj_buf.visibleOutline; }
uint32_t occt_get_proj_visible_smooth() { return g_proj_buf.visibleSmooth; }
uint32_t occt_get_proj_visible_sharp() { return g_proj_buf.visibleSharp; }
uint32_t occt_get_proj_hidden_outline() { return g_proj_buf.hiddenOutline; }
uint32_t occt_get_proj_hidden_smooth() { return g_proj_buf.hiddenSmooth; }
uint32_t occt_get_proj_hidden_sharp() { return g_proj_buf.hiddenSharp; }

// XCAF label info result
int32_t occt_get_label_info_label_id() { return g_label_info_buf.labelId; }
const char* occt_get_label_info_name() { return g_label_info_buf.name.c_str(); }
uint32_t occt_get_label_info_name_len() { return static_cast<uint32_t>(g_label_info_buf.name.size()); }
int32_t occt_get_label_info_has_color() { return g_label_info_buf.hasColor ? 1 : 0; }
double occt_get_label_info_r() { return g_label_info_buf.r; }
double occt_get_label_info_g() { return g_label_info_buf.g; }
double occt_get_label_info_b() { return g_label_info_buf.b; }
int32_t occt_get_label_info_is_assembly() { return g_label_info_buf.isAssembly ? 1 : 0; }
int32_t occt_get_label_info_is_component() { return g_label_info_buf.isComponent ? 1 : 0; }
uint32_t occt_get_label_info_shape_id() { return g_label_info_buf.shapeId; }

// === Kernel methods ===

uint32_t occt_make_box(double dx, double dy, double dz) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeBox(dx, dy, dz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_box_from_corners(double x1, double y1, double z1, double x2, double y2, double z2) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeBoxFromCorners(x1, y1, z1, x2, y2, z2);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_cylinder(double radius, double height) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeCylinder(radius, height);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_sphere(double radius) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeSphere(radius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_cone(double r1, double r2, double height) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeCone(r1, r2, height);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_torus(double majorRadius, double minorRadius) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeTorus(majorRadius, minorRadius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_ellipsoid(double rx, double ry, double rz) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeEllipsoid(rx, ry, rz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_half_space(double ox, double oy, double oz, double nx, double ny, double nz) {
    CLEAR_ERROR();
    try {
        return g_kernel->halfSpace(ox, oy, oz, nx, ny, nz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_rectangle(double width, double height) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeRectangle(width, height);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_fuse(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->fuse(a, b);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_cut(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->cut(a, b);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_common(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->common(a, b);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_section(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->section(a, b);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_intersect(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->intersect(a, b);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_fuse_all(const uint32_t* shapeIds_ptr, uint32_t shapeIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->fuseAll(std::vector<uint32_t>(shapeIds_ptr, shapeIds_ptr + shapeIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_intersection_cells(const uint32_t* shapeIds_ptr, uint32_t shapeIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->intersectionCells(std::vector<uint32_t>(shapeIds_ptr, shapeIds_ptr + shapeIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_cut_all(uint32_t shapeId, const uint32_t* toolIds_ptr, uint32_t toolIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->cutAll(shapeId, std::vector<uint32_t>(toolIds_ptr, toolIds_ptr + toolIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_boolean_pipeline(uint32_t baseId, const int32_t* opCodes_ptr, uint32_t opCodes_len, const uint32_t* toolIds_ptr, uint32_t toolIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->booleanPipeline(baseId, std::vector<int>(opCodes_ptr, opCodes_ptr + opCodes_len), std::vector<uint32_t>(toolIds_ptr, toolIds_ptr + toolIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_boolean_fuzzy(uint32_t a, uint32_t b, int32_t opCode, double fuzz) {
    CLEAR_ERROR();
    try {
        return g_kernel->booleanFuzzy(a, b, opCode, fuzz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_split(uint32_t shapeId, const uint32_t* toolIds_ptr, uint32_t toolIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->split(shapeId, std::vector<uint32_t>(toolIds_ptr, toolIds_ptr + toolIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_extrude(uint32_t shapeId, double dx, double dy, double dz) {
    CLEAR_ERROR();
    try {
        return g_kernel->extrude(shapeId, dx, dy, dz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_revolve(uint32_t shapeId, double px, double py, double pz, double dx, double dy, double dz, double angleRad) {
    CLEAR_ERROR();
    try {
        return g_kernel->revolve(shapeId, px, py, pz, dx, dy, dz, angleRad);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_fillet(uint32_t solidId, const uint32_t* edgeIds_ptr, uint32_t edgeIds_len, double radius) {
    CLEAR_ERROR();
    try {
        return g_kernel->fillet(solidId, std::vector<uint32_t>(edgeIds_ptr, edgeIds_ptr + edgeIds_len), radius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_chamfer(uint32_t solidId, const uint32_t* edgeIds_ptr, uint32_t edgeIds_len, double distance) {
    CLEAR_ERROR();
    try {
        return g_kernel->chamfer(solidId, std::vector<uint32_t>(edgeIds_ptr, edgeIds_ptr + edgeIds_len), distance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_chamfer_dist_angle(uint32_t solidId, const uint32_t* edgeIds_ptr, uint32_t edgeIds_len, double distance, double angleDeg) {
    CLEAR_ERROR();
    try {
        return g_kernel->chamferDistAngle(solidId, std::vector<uint32_t>(edgeIds_ptr, edgeIds_ptr + edgeIds_len), distance, angleDeg);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_shell(uint32_t solidId, const uint32_t* faceIds_ptr, uint32_t faceIds_len, double thickness, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->shell(solidId, std::vector<uint32_t>(faceIds_ptr, faceIds_ptr + faceIds_len), thickness, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_offset(uint32_t solidId, double distance, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->offset(solidId, distance, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_draft(uint32_t shapeId, uint32_t faceId, double angleRad, double dx, double dy, double dz) {
    CLEAR_ERROR();
    try {
        return g_kernel->draft(shapeId, faceId, angleRad, dx, dy, dz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_thicken(uint32_t shapeId, double thickness, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->thicken(shapeId, thickness, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_defeature(uint32_t shapeId, const uint32_t* faceIds_ptr, uint32_t faceIds_len, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->defeature(shapeId, std::vector<uint32_t>(faceIds_ptr, faceIds_ptr + faceIds_len), tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_reverse_shape(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->reverseShape(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_simplify(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->simplify(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_fillet_variable(uint32_t solidId, uint32_t edgeId, double startRadius, double endRadius) {
    CLEAR_ERROR();
    try {
        return g_kernel->filletVariable(solidId, edgeId, startRadius, endRadius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_fillet_batch(const uint32_t* solidIds_ptr, uint32_t solidIds_len, const int32_t* edgeCounts_ptr, uint32_t edgeCounts_len, const uint32_t* flatEdgeIds_ptr, uint32_t flatEdgeIds_len, const double* radii_ptr, uint32_t radii_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->filletBatch(std::vector<uint32_t>(solidIds_ptr, solidIds_ptr + solidIds_len), std::vector<int>(edgeCounts_ptr, edgeCounts_ptr + edgeCounts_len), std::vector<uint32_t>(flatEdgeIds_ptr, flatEdgeIds_ptr + flatEdgeIds_len), std::vector<double>(radii_ptr, radii_ptr + radii_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_offset_wire2_d(uint32_t wireId, double offset, int32_t joinType) {
    CLEAR_ERROR();
    try {
        return g_kernel->offsetWire2D(wireId, offset, joinType);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_translate(uint32_t id, double dx, double dy, double dz) {
    CLEAR_ERROR();
    try {
        return g_kernel->translate(id, dx, dy, dz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_rotate(uint32_t id, double px, double py, double pz, double dx, double dy, double dz, double angleRad) {
    CLEAR_ERROR();
    try {
        return g_kernel->rotate(id, px, py, pz, dx, dy, dz, angleRad);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_scale(uint32_t id, double px, double py, double pz, double factor) {
    CLEAR_ERROR();
    try {
        return g_kernel->scale(id, px, py, pz, factor);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_mirror(uint32_t id, double px, double py, double pz, double nx, double ny, double nz) {
    CLEAR_ERROR();
    try {
        return g_kernel->mirror(id, px, py, pz, nx, ny, nz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_copy(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->copy(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_linear_pattern(uint32_t id, double dx, double dy, double dz, double spacing, int32_t count) {
    CLEAR_ERROR();
    try {
        return g_kernel->linearPattern(id, dx, dy, dz, spacing, count);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_circular_pattern(uint32_t id, double cx, double cy, double cz, double ax, double ay, double az, double angle, int32_t count) {
    CLEAR_ERROR();
    try {
        return g_kernel->circularPattern(id, cx, cy, cz, ax, ay, az, angle, count);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_transform(uint32_t id, const double* matrix_ptr, uint32_t matrix_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->transform(id, std::vector<double>(matrix_ptr, matrix_ptr + matrix_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_located(uint32_t id, const double* matrix_ptr, uint32_t matrix_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->located(id, std::vector<double>(matrix_ptr, matrix_ptr + matrix_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_general_transform(uint32_t id, const double* matrix_ptr, uint32_t matrix_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->generalTransform(id, std::vector<double>(matrix_ptr, matrix_ptr + matrix_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_translate_batch(const uint32_t* ids_ptr, uint32_t ids_len, const double* offsets_ptr, uint32_t offsets_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->translateBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len), std::vector<double>(offsets_ptr, offsets_ptr + offsets_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_compose_transform(const double* m1_ptr, uint32_t m1_len, const double* m2_ptr, uint32_t m2_len) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->composeTransform(std::vector<double>(m1_ptr, m1_ptr + m1_len), std::vector<double>(m2_ptr, m2_ptr + m2_len));
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_transform_batch(const uint32_t* ids_ptr, uint32_t ids_len, const double* matrices_ptr, uint32_t matrices_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->transformBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len), std::vector<double>(matrices_ptr, matrices_ptr + matrices_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_rotate_batch(const uint32_t* ids_ptr, uint32_t ids_len, const double* params_ptr, uint32_t params_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->rotateBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len), std::vector<double>(params_ptr, params_ptr + params_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_scale_batch(const uint32_t* ids_ptr, uint32_t ids_len, const double* params_ptr, uint32_t params_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->scaleBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len), std::vector<double>(params_ptr, params_ptr + params_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_mirror_batch(const uint32_t* ids_ptr, uint32_t ids_len, const double* params_ptr, uint32_t params_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->mirrorBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len), std::vector<double>(params_ptr, params_ptr + params_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_make_vertex(double x, double y, double z) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeVertex(x, y, z);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_edge(uint32_t v1, uint32_t v2) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeEdge(v1, v2);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_wire(const uint32_t* edgeIds_ptr, uint32_t edgeIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeWire(std::vector<uint32_t>(edgeIds_ptr, edgeIds_ptr + edgeIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_face(uint32_t wireId) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeFace(wireId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_face_on_surface(uint32_t faceId, uint32_t wireId) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeFaceOnSurface(faceId, wireId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_solid(uint32_t shellId) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeSolid(shellId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_sew(const uint32_t* shapeIds_ptr, uint32_t shapeIds_len, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->sew(std::vector<uint32_t>(shapeIds_ptr, shapeIds_ptr + shapeIds_len), tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_compound(const uint32_t* shapeIds_ptr, uint32_t shapeIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeCompound(std::vector<uint32_t>(shapeIds_ptr, shapeIds_ptr + shapeIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_line_edge(double x1, double y1, double z1, double x2, double y2, double z2) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeLineEdge(x1, y1, z1, x2, y2, z2);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_circle_edge(double cx, double cy, double cz, double nx, double ny, double nz, double radius) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeCircleEdge(cx, cy, cz, nx, ny, nz, radius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_circle_arc(double cx, double cy, double cz, double nx, double ny, double nz, double radius, double startAngle, double endAngle) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeCircleArc(cx, cy, cz, nx, ny, nz, radius, startAngle, endAngle);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_arc_edge(double x1, double y1, double z1, double x2, double y2, double z2, double x3, double y3, double z3) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeArcEdge(x1, y1, z1, x2, y2, z2, x3, y3, z3);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_ellipse_edge(double cx, double cy, double cz, double nx, double ny, double nz, double majorRadius, double minorRadius) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeEllipseEdge(cx, cy, cz, nx, ny, nz, majorRadius, minorRadius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_bezier_edge(const double* flatPoints_ptr, uint32_t flatPoints_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeBezierEdge(std::vector<double>(flatPoints_ptr, flatPoints_ptr + flatPoints_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_b_spline_edge(const double* poles_ptr, uint32_t poles_len, const double* weights_ptr, uint32_t weights_len, const double* knots_ptr, uint32_t knots_len, const int32_t* multiplicities_ptr, uint32_t multiplicities_len, int32_t degree, int32_t periodic) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeBSplineEdge(std::vector<double>(poles_ptr, poles_ptr + poles_len), std::vector<double>(weights_ptr, weights_ptr + weights_len), std::vector<double>(knots_ptr, knots_ptr + knots_len), std::vector<int>(multiplicities_ptr, multiplicities_ptr + multiplicities_len), degree, (periodic != 0));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_ellipse_arc(double cx, double cy, double cz, double nx, double ny, double nz, double majorRadius, double minorRadius, double startAngle, double endAngle) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeEllipseArc(cx, cy, cz, nx, ny, nz, majorRadius, minorRadius, startAngle, endAngle);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_helix_wire(double px, double py, double pz, double dx, double dy, double dz, double pitch, double height, double radius) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeHelixWire(px, py, pz, dx, dy, dz, pitch, height, radius);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_non_planar_face(uint32_t wireId) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeNonPlanarFace(wireId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_add_holes_in_face(uint32_t faceId, const uint32_t* holeWireIds_ptr, uint32_t holeWireIds_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->addHolesInFace(faceId, std::vector<uint32_t>(holeWireIds_ptr, holeWireIds_ptr + holeWireIds_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_remove_holes_from_face(uint32_t faceId, const int32_t* holeIndices_ptr, uint32_t holeIndices_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->removeHolesFromFace(faceId, std::vector<int>(holeIndices_ptr, holeIndices_ptr + holeIndices_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_solid_from_shell(uint32_t shellId) {
    CLEAR_ERROR();
    try {
        return g_kernel->solidFromShell(shellId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_build_solid_from_faces(const uint32_t* faceIds_ptr, uint32_t faceIds_len, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->buildSolidFromFaces(std::vector<uint32_t>(faceIds_ptr, faceIds_ptr + faceIds_len), tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_sew_and_solidify(const uint32_t* faceIds_ptr, uint32_t faceIds_len, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->sewAndSolidify(std::vector<uint32_t>(faceIds_ptr, faceIds_ptr + faceIds_len), tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_build_tri_face(double ax, double ay, double az, double bx, double by, double bz, double cx2, double cy2, double cz2) {
    CLEAR_ERROR();
    try {
        return g_kernel->buildTriFace(ax, ay, az, bx, by, bz, cx2, cy2, cz2);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_tangent_arc(double x1, double y1, double z1, double tx, double ty, double tz, double x2, double y2, double z2) {
    CLEAR_ERROR();
    try {
        return g_kernel->makeTangentArc(x1, y1, z1, tx, ty, tz, x2, y2, z2);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_bspline_surface(const double* flatPoints_ptr, uint32_t flatPoints_len, int32_t rows, int32_t cols) {
    CLEAR_ERROR();
    try {
        return g_kernel->bsplineSurface(std::vector<double>(flatPoints_ptr, flatPoints_ptr + flatPoints_len), rows, cols);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_get_shape_type(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->getShapeType(id);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_get_sub_shapes(uint32_t id, const char* shapeType_ptr, uint32_t shapeType_len) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->getSubShapes(id, std::string(shapeType_ptr, shapeType_len));
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_sub_shape_count(uint32_t id, const char* shapeType_ptr, uint32_t shapeType_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->subShapeCount(id, std::string(shapeType_ptr, shapeType_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_sub_shape_hashes(uint32_t id, const char* shapeType_ptr, uint32_t shapeType_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_vec_i32_buf = g_kernel->subShapeHashes(id, std::string(shapeType_ptr, shapeType_len), hashUpperBound);
        return static_cast<int32_t>(g_vec_i32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

double occt_distance_between(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->distanceBetween(a, b);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return std::numeric_limits<double>::quiet_NaN();
    }
}

int32_t occt_is_same(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->isSame(a, b) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_is_equal(uint32_t a, uint32_t b) {
    CLEAR_ERROR();
    try {
        return g_kernel->isEqual(a, b) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_is_null(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->isNull(id) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_hash_code(uint32_t id, int32_t upperBound) {
    CLEAR_ERROR();
    try {
        return g_kernel->hashCode(id, upperBound);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_shape_orientation(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->shapeOrientation(id);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_iter_shapes(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->iterShapes(id);
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_edge_to_face_map(uint32_t id, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_vec_i32_buf = g_kernel->edgeToFaceMap(id, hashUpperBound);
        return static_cast<int32_t>(g_vec_i32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_downcast(uint32_t id, const char* targetType_ptr, uint32_t targetType_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->downcast(id, std::string(targetType_ptr, targetType_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_adjacent_faces(uint32_t shapeId, uint32_t faceId) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->adjacentFaces(shapeId, faceId);
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_shared_edges(uint32_t faceA, uint32_t faceB) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->sharedEdges(faceA, faceB);
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_get_bounding_box(uint32_t id, int32_t useTriangulation) {
    CLEAR_ERROR();
    try {
        g_bbox_buf = g_kernel->getBoundingBox(id, (useTriangulation != 0));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

double occt_get_volume(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->getVolume(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return std::numeric_limits<double>::quiet_NaN();
    }
}

double occt_get_surface_area(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->getSurfaceArea(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return std::numeric_limits<double>::quiet_NaN();
    }
}

double occt_get_length(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->getLength(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return std::numeric_limits<double>::quiet_NaN();
    }
}

int32_t occt_get_center_of_mass(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->getCenterOfMass(id);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_get_inertia(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->getInertia(id);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_contains_point(uint32_t id, double x, double y, double z, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->containsPoint(id, x, y, z, tolerance) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_get_surface_center_of_mass(uint32_t faceId) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->getSurfaceCenterOfMass(faceId);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_vertex_position(uint32_t vertexId) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->vertexPosition(vertexId);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_surface_type(uint32_t faceId) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->surfaceType(faceId);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_surface_normal(uint32_t faceId, double u, double v) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->surfaceNormal(faceId, u, v);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_point_on_surface(uint32_t faceId, double u, double v) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->pointOnSurface(faceId, u, v);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_outer_wire(uint32_t faceId) {
    CLEAR_ERROR();
    try {
        return g_kernel->outerWire(faceId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_get_linear_center_of_mass(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->getLinearCenterOfMass(id);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_surface_curvature(uint32_t faceId, double u, double v) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->surfaceCurvature(faceId, u, v);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_uv_bounds(uint32_t faceId) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->uvBounds(faceId);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_get_face_cylinder_data(uint32_t faceId) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->getFaceCylinderData(faceId);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_reverse_surface_u(uint32_t faceId) {
    CLEAR_ERROR();
    try {
        return g_kernel->reverseSurfaceU(faceId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_uv_from_point(uint32_t faceId, double x, double y, double z) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->uvFromPoint(faceId, x, y, z);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_project_point_on_face(uint32_t faceId, double x, double y, double z) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->projectPointOnFace(faceId, x, y, z);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_classify_point_on_face(uint32_t faceId, double u, double v) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->classifyPointOnFace(faceId, u, v);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_curve_type(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->curveType(id);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_curve_point_at_param(uint32_t id, double param) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->curvePointAtParam(id, param);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_curve_tangent(uint32_t id, double param) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->curveTangent(id, param);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_curve_parameters(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->curveParameters(id);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_curve_is_closed(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->curveIsClosed(id) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

double occt_curve_length(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->curveLength(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return std::numeric_limits<double>::quiet_NaN();
    }
}

uint32_t occt_interpolate_points(const double* flatPoints_ptr, uint32_t flatPoints_len, int32_t periodic) {
    CLEAR_ERROR();
    try {
        return g_kernel->interpolatePoints(std::vector<double>(flatPoints_ptr, flatPoints_ptr + flatPoints_len), (periodic != 0));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_interpolate_points_with_tangents(const double* flatPoints_ptr, uint32_t flatPoints_len, double startTanX, double startTanY, double startTanZ, double endTanX, double endTanY, double endTanZ) {
    CLEAR_ERROR();
    try {
        return g_kernel->interpolatePointsWithTangents(std::vector<double>(flatPoints_ptr, flatPoints_ptr + flatPoints_len), startTanX, startTanY, startTanZ, endTanX, endTanY, endTanZ);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_project_point_on_edge(uint32_t edgeId, double x, double y, double z) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->projectPointOnEdge(edgeId, x, y, z);
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_curve_is_periodic(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->curveIsPeriodic(id) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_approximate_points(const double* flatPoints_ptr, uint32_t flatPoints_len, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->approximatePoints(std::vector<double>(flatPoints_ptr, flatPoints_ptr + flatPoints_len), tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_lift_curve2d_to_plane(const double* flatPoints2d_ptr, uint32_t flatPoints2d_len, double planeOx, double planeOy, double planeOz, double planeZx, double planeZy, double planeZz, double planeXx, double planeXy, double planeXz) {
    CLEAR_ERROR();
    try {
        return g_kernel->liftCurve2dToPlane(std::vector<double>(flatPoints2d_ptr, flatPoints2d_ptr + flatPoints2d_len), planeOx, planeOy, planeOz, planeZx, planeZy, planeZz, planeXx, planeXy, planeXz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_get_nurbs_curve_data(uint32_t edgeId) {
    CLEAR_ERROR();
    try {
        g_nurbs_buf = g_kernel->getNurbsCurveData(edgeId);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_curve_degree_elevate(uint32_t edgeId, int32_t elevateBy) {
    CLEAR_ERROR();
    try {
        return g_kernel->curveDegreeElevate(edgeId, elevateBy);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_curve_knot_insert(uint32_t edgeId, double knot, int32_t times) {
    CLEAR_ERROR();
    try {
        return g_kernel->curveKnotInsert(edgeId, knot, times);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_curve_knot_remove(uint32_t edgeId, double knot, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->curveKnotRemove(edgeId, knot, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_curve_split(uint32_t edgeId, double param) {
    CLEAR_ERROR();
    try {
        g_vec_u32_buf = g_kernel->curveSplit(edgeId, param);
        return static_cast<int32_t>(g_vec_u32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_has_triangulation(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->hasTriangulation(id) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_query_batch(const uint32_t* ids_ptr, uint32_t ids_len) {
    CLEAR_ERROR();
    try {
        g_vec_f64_buf = g_kernel->queryBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len));
        return static_cast<int32_t>(g_vec_f64_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_pipe(uint32_t profileId, uint32_t spineId) {
    CLEAR_ERROR();
    try {
        return g_kernel->pipe(profileId, spineId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_simple_pipe(uint32_t profileId, uint32_t spineId) {
    CLEAR_ERROR();
    try {
        return g_kernel->simplePipe(profileId, spineId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_revolve_vec(uint32_t shapeId, double cx, double cy, double cz, double dx, double dy, double dz, double angle) {
    CLEAR_ERROR();
    try {
        return g_kernel->revolveVec(shapeId, cx, cy, cz, dx, dy, dz, angle);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_loft(const uint32_t* wireIds_ptr, uint32_t wireIds_len, int32_t isSolid, int32_t ruled) {
    CLEAR_ERROR();
    try {
        return g_kernel->loft(std::vector<uint32_t>(wireIds_ptr, wireIds_ptr + wireIds_len), (isSolid != 0), (ruled != 0));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_loft_with_vertices(const uint32_t* wireIds_ptr, uint32_t wireIds_len, int32_t isSolid, int32_t ruled, uint32_t startVertexId, uint32_t endVertexId) {
    CLEAR_ERROR();
    try {
        return g_kernel->loftWithVertices(std::vector<uint32_t>(wireIds_ptr, wireIds_ptr + wireIds_len), (isSolid != 0), (ruled != 0), startVertexId, endVertexId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_sweep(uint32_t wireId, uint32_t spineId, int32_t transitionMode) {
    CLEAR_ERROR();
    try {
        return g_kernel->sweep(wireId, spineId, transitionMode);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_sweep_pipe_shell(uint32_t profileId, uint32_t spineId, int32_t freenet, int32_t smooth) {
    CLEAR_ERROR();
    try {
        return g_kernel->sweepPipeShell(profileId, spineId, (freenet != 0), (smooth != 0));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_sweep_oriented(uint32_t profileId, uint32_t spineId, int32_t mode, double upX, double upY, double upZ, uint32_t auxSpineId) {
    CLEAR_ERROR();
    try {
        return g_kernel->sweepOriented(profileId, spineId, mode, upX, upY, upZ, auxSpineId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_draft_prism(uint32_t shapeId, double dx, double dy, double dz, double angleDeg) {
    CLEAR_ERROR();
    try {
        return g_kernel->draftPrism(shapeId, dx, dy, dz, angleDeg);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_fix_shape(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->fixShape(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_unify_same_domain(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->unifySameDomain(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_is_valid(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->isValid(id) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_check_shape(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->checkShape(id) ? 1 : 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_heal_solid(uint32_t id, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->healSolid(id, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_heal_face(uint32_t id, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->healFace(id, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_heal_wire(uint32_t id, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->healWire(id, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_fix_face_orientations(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->fixFaceOrientations(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_build_curves3d(uint32_t wireId) {
    CLEAR_ERROR();
    try {
        g_kernel->buildCurves3d(wireId);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_fix_wire_on_face(uint32_t wireId, uint32_t faceId, double tolerance) {
    CLEAR_ERROR();
    try {
        return g_kernel->fixWireOnFace(wireId, faceId, tolerance);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_remove_degenerate_edges(uint32_t id) {
    CLEAR_ERROR();
    try {
        return g_kernel->removeDegenerateEdges(id);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_import_step(const char* data_ptr, uint32_t data_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->importStep(std::string(data_ptr, data_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_export_step(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->exportStep(id);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_export_stl(uint32_t id, double linearDeflection, int32_t ascii) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->exportStl(id, linearDeflection, (ascii != 0));
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_import_stl(const char* data_ptr, uint32_t data_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->importStl(std::string(data_ptr, data_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_to_brep(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->toBREP(id);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_from_brep(const char* data_ptr, uint32_t data_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->fromBREP(std::string(data_ptr, data_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_export_brep_binary(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->exportBrepBinary(id);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_import_brep_binary(const char* path_ptr, uint32_t path_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->importBrepBinary(std::string(path_ptr, path_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_translate_with_history(uint32_t id, double dx, double dy, double dz, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->translateWithHistory(id, dx, dy, dz, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_fuse_with_history(uint32_t a, uint32_t b, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->fuseWithHistory(a, b, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_cut_with_history(uint32_t a, uint32_t b, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->cutWithHistory(a, b, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_fillet_with_history(uint32_t solidId, const uint32_t* edgeIds_ptr, uint32_t edgeIds_len, double radius, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->filletWithHistory(solidId, std::vector<uint32_t>(edgeIds_ptr, edgeIds_ptr + edgeIds_len), radius, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_rotate_with_history(uint32_t id, double px, double py, double pz, double dx, double dy, double dz, double angle, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->rotateWithHistory(id, px, py, pz, dx, dy, dz, angle, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_mirror_with_history(uint32_t id, double px, double py, double pz, double nx, double ny, double nz, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->mirrorWithHistory(id, px, py, pz, nx, ny, nz, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_scale_with_history(uint32_t id, double cx, double cy, double cz, double factor, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->scaleWithHistory(id, cx, cy, cz, factor, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_intersect_with_history(uint32_t a, uint32_t b, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->intersectWithHistory(a, b, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_chamfer_with_history(uint32_t solidId, const uint32_t* edgeIds_ptr, uint32_t edgeIds_len, double distance, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->chamferWithHistory(solidId, std::vector<uint32_t>(edgeIds_ptr, edgeIds_ptr + edgeIds_len), distance, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_shell_with_history(uint32_t solidId, const uint32_t* faceIds_ptr, uint32_t faceIds_len, double thickness, double tolerance, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->shellWithHistory(solidId, std::vector<uint32_t>(faceIds_ptr, faceIds_ptr + faceIds_len), thickness, tolerance, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_offset_with_history(uint32_t solidId, double distance, double tolerance, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->offsetWithHistory(solidId, distance, tolerance, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_thicken_with_history(uint32_t shapeId, double thickness, double tolerance, const int32_t* inputFaceHashes_ptr, uint32_t inputFaceHashes_len, int32_t hashUpperBound) {
    CLEAR_ERROR();
    try {
        g_evo_buf = g_kernel->thickenWithHistory(shapeId, thickness, tolerance, std::vector<int>(inputFaceHashes_ptr, inputFaceHashes_ptr + inputFaceHashes_len), hashUpperBound);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_tessellate(uint32_t id, double linearDeflection, double angularDeflection) {
    CLEAR_ERROR();
    try {
        g_mesh_buf.~MeshData();
        new (&g_mesh_buf) MeshData(g_kernel->tessellate(id, linearDeflection, angularDeflection));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_tessellate_relative(uint32_t id, double linearDeflection, double angularDeflection) {
    CLEAR_ERROR();
    try {
        g_mesh_buf.~MeshData();
        new (&g_mesh_buf) MeshData(g_kernel->tessellateRelative(id, linearDeflection, angularDeflection));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_mesh_shape(uint32_t id, double linearDeflection, double angularDeflection) {
    CLEAR_ERROR();
    try {
        g_mesh_buf.~MeshData();
        new (&g_mesh_buf) MeshData(g_kernel->meshShape(id, linearDeflection, angularDeflection));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_mesh_batch(const uint32_t* ids_ptr, uint32_t ids_len, double linearDeflection, double angularDeflection) {
    CLEAR_ERROR();
    try {
        g_mesh_batch_buf.~MeshBatchData();
        new (&g_mesh_batch_buf) MeshBatchData(g_kernel->meshBatch(std::vector<uint32_t>(ids_ptr, ids_ptr + ids_len), linearDeflection, angularDeflection));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_wireframe(uint32_t id, double deflection) {
    CLEAR_ERROR();
    try {
        g_edge_buf.~EdgeData();
        new (&g_edge_buf) EdgeData(g_kernel->wireframe(id, deflection));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_project_edges(uint32_t shapeId, double ox, double oy, double oz, double dx, double dy, double dz, double xx, double xy, double xz, int32_t hasXAxis) {
    CLEAR_ERROR();
    try {
        g_proj_buf = g_kernel->projectEdges(shapeId, ox, oy, oz, dx, dy, dz, xx, xy, xz, (hasXAxis != 0));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_release(uint32_t id) {
    CLEAR_ERROR();
    try {
        g_kernel->release(id);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_release_all() {
    CLEAR_ERROR();
    try {
        g_kernel->releaseAll();
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_checkpoint() {
    CLEAR_ERROR();
    try {
        return g_kernel->checkpoint();
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_release_since(uint32_t mark) {
    CLEAR_ERROR();
    try {
        g_kernel->releaseSince(mark);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_get_shape_count() {
    CLEAR_ERROR();
    try {
        return g_kernel->getShapeCount();
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_make_null_shape() {
    CLEAR_ERROR();
    try {
        return g_kernel->makeNullShape();
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

uint32_t occt_xcaf_new_document() {
    CLEAR_ERROR();
    try {
        return g_kernel->xcafNewDocument();
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_xcaf_close(uint32_t docId) {
    CLEAR_ERROR();
    try {
        g_kernel->xcafClose(docId);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_add_shape(uint32_t docId, uint32_t shapeId) {
    CLEAR_ERROR();
    try {
        return g_kernel->xcafAddShape(docId, shapeId);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_add_component(uint32_t docId, int32_t parentLabelId, uint32_t shapeId, double tx, double ty, double tz, double rx, double ry, double rz) {
    CLEAR_ERROR();
    try {
        return g_kernel->xcafAddComponent(docId, parentLabelId, shapeId, tx, ty, tz, rx, ry, rz);
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_set_color(uint32_t docId, int32_t labelId, double r, double g, double b) {
    CLEAR_ERROR();
    try {
        g_kernel->xcafSetColor(docId, labelId, r, g, b);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_set_name(uint32_t docId, int32_t labelId, const char* name_ptr, uint32_t name_len) {
    CLEAR_ERROR();
    try {
        g_kernel->xcafSetName(docId, labelId, std::string(name_ptr, name_len));
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_get_label_info(uint32_t docId, int32_t labelId) {
    CLEAR_ERROR();
    try {
        g_label_info_buf = g_kernel->xcafGetLabelInfo(docId, labelId);
        return 0;
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_get_child_labels(uint32_t docId, int32_t parentLabelId) {
    CLEAR_ERROR();
    try {
        g_vec_i32_buf = g_kernel->xcafGetChildLabels(docId, parentLabelId);
        return static_cast<int32_t>(g_vec_i32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_get_root_labels(uint32_t docId) {
    CLEAR_ERROR();
    try {
        g_vec_i32_buf = g_kernel->xcafGetRootLabels(docId);
        return static_cast<int32_t>(g_vec_i32_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

int32_t occt_xcaf_export_step(uint32_t docId) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->xcafExportSTEP(docId);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

uint32_t occt_xcaf_import_step(const char* stepData_ptr, uint32_t stepData_len) {
    CLEAR_ERROR();
    try {
        return g_kernel->xcafImportSTEP(std::string(stepData_ptr, stepData_len));
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return 0;
    }
}

int32_t occt_xcaf_export_gltf(uint32_t docId, double linDeflection, double angDeflection) {
    CLEAR_ERROR();
    try {
        g_string_buf = g_kernel->xcafExportGLTF(docId, linDeflection, angDeflection);
        return static_cast<int32_t>(g_string_buf.size());
    } catch (const std::exception& e) {
        SET_ERROR(e.what());
        return -1;
    }
}

} // extern "C"
